import { styled, ImageList } from "@mui/material";

const CustomImageList = styled(ImageList)({
	alignContent: 'space-around',
	justifyItems: 'center',
	height: 'auto',
	width: 'auto',
	padding: '20px'
});

export default CustomImageList;